package com.altude.provenance.data

/**
 * Container for provenance-related request types.
 *
 * Add new request data classes or helper functions as nested types or
 * members of this object to keep provenance request definitions
 * organized in a single place.
 */
object ProvenanceRequests

/**
 * Marker interface for provenance-related request DTOs.
 *
 * Add concrete request models in this package implementing [ProvenanceRequest]
 * to ensure a consistent type hierarchy for provenance operations.
 */
internal interface ProvenanceRequest
