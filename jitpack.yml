jdk:
  - openjdk11
before_install:
  - sdk install java 11.0.2-open
  - sdk use java 11.0.2-open
install:
  - echo "Running JitPack build for private repository"
  - ./gradlew publishToMavenLocal -x test